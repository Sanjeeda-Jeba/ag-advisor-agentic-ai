# Plan: Organize Legacy Markdown Files

1. **Inventory Files**
   - List all `.md` files at repo root.
   - Identify ones to keep at root (`README.md`, `AGENTIC_AI_PROJECT_OVERVIEW.md`).

2. **Create Archive Folder**
   - Add `docs/archive/` (or similar) to store historical plans/summaries.
   - Ensure folder is tracked (add `README` placeholder if needed).

3. **Move Markdown Files**
   - Relocate every other `.md` into `docs/archive/`.
   - Preserve filenames for reference.

4. **Update References (optional)**
   - If `README` or other docs link to these files, adjust paths.

5. **Verify Cleanup**
   - List root to confirm only intended `.md` remain.
   - Note the new structure in project overview if necessary.
